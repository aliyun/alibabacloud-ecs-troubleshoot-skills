# ECS Linux 诊断报告

## 1. 基本信息

| 项 | 值 |
| --- | --- |
| 实例 ID | `i-xxxxxxxx` |
| 地域 | `cn-xxx` |
| 实例规格 | `ecs.xxx` |
| 镜像 | `<imageId> / <OS 名称与版本>` |
| 现象域大类 | `<启动类 / 使用类 / 宕机或夯机类 / 性能类 / ...>` |
| 现象域 | `<实例已经处于「运行中」状态，但 GuestOS 没有正常启动 / ...>` |
| 报告时间 | `YYYY-MM-DD HH:MM` |

## 2. 问题描述

- **用户原始描述**：<原文复述，1-3 句>
- **澄清后的问题**：<归一化后的现象描述>

## 3. 根本原因

- **根因 1**：<一句话定性，例如「`/var` 所在数据盘 inode 耗尽，导致 sshd 写入临时文件失败」>
  - 证据：<排查过程中发现的根因导致问题现象的证据>
- **根因 2**（如有）：<...>

## 5. 修复与规避建议

### 5.1 立即修复

1. <动作描述>

   ```bash
   <完整命令>
   ```

2. ...

### 5.2 长期规避

- <配置/监控/容量/版本等层面的预防措施>
